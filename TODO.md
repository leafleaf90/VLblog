# TODO

## Security / repo hygiene

- It did not run TruffleHog, gitleaks, or a full binary scan. If you want stronger assurance, run one of those on a full clone (they catch more edge cases than a few `git log -S` checks).
